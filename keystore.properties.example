# Copy to keystore.properties and fill in your release signing credentials.
# keystore.properties is gitignored and must never be committed.
storeFile=/path/to/luteal-release.jks
storePassword=
keyAlias=luteal-release
keyPassword=
